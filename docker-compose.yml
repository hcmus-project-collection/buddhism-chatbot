services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: eastern-religion-backend
    environment:
      - PORT=8000
    volumes:
      - ./.env:/app/.env:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.eastern-backend.rule=Host(`backend-easternchatbot.nguyenvanloc.com`)"
      - "traefik.http.routers.eastern-backend.entrypoints=websecure"
      - "traefik.http.routers.eastern-backend.tls=true"
      - "traefik.http.services.eastern-backend.loadbalancer.server.port=8000"
    networks:
      - localnet
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: eastern-religion-frontend
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=https://backend-easternchatbot.nguyenvanloc.com
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.eastern-frontend.rule=Host(`eastern-chatbot.nguyenvanloc.com`)"
      - "traefik.http.routers.eastern-frontend.entrypoints=websecure"
      - "traefik.http.routers.eastern-frontend.tls=true"
      - "traefik.http.services.frontend.loadbalancer.server.port=3000"
    networks:
      - localnet
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

networks:
  localnet:
    external: true
